# Relay state

The relay writes public job metadata to `jobs.json` in this directory by default. JSON state files are ignored by Git. Set `RELAY_STATE_FILE` to use a different location.

Never place private keys or credentials in this directory.
