# Contracts

ProofKey's Solidity contracts and deployment modules live here. The compiler is pinned to Solidity `0.8.28` in `hardhat.config.ts`.

Issue #2 introduces the first contract and its tests; keeping this foundation issue free of protocol behavior makes later security review easier.
