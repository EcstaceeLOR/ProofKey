# Live precompile evidence

This directory is reserved for fixtures captured from Creditcoin's real Native Query Verifier at `0x0FD2`. No live fixture has been committed yet.

Recorded fixtures must be labeled as recorded evidence and include reproducible source and Creditcoin explorer links. They must never be mixed with the local verifier doubles documented in the parent test directory.
